# ──────────────────────────────────────────────────────────────
# ✅ Histórico de alterações:
# [2025-06-02] Corrigido erro EACCES em ~/.npm/_logs com chown
# Adicionados debugs úteis: `npm whoami`, `git remote -v`, `curl GitHub API`
# Mantido uso de npm install (evita falhas com semantic-release)
# ──────────────────────────────────────────────────────────────

name: 📦 Release

on:
  push:
    branches:
      - main

permissions:
  contents: write
  issues: write
  pull-requests: write
  id-token: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: 🔄 Checkout do código
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node.js e autenticação NPM
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: 'https://registry.npmjs.org/'
          always-auth: true
          token: ${{ secrets.NPM_TOKEN }}

      - name: 📦 Instalar dependências
        run: npm install

      - name: 🔐 Configurar .npmrc com token
        run: |
          echo "//registry.npmjs.org/:_authToken=${{ secrets.NPM_TOKEN }}" > ~/.npmrc
          echo "Conteúdo do .npmrc:"
          cat ~/.npmrc
          echo "Verificando autenticação com npm whoami..."
          npm whoami || echo "Falha ao autenticar no NPM, confira o token"

      - name: ⚙️ Corrigir permissões do diretório ~/.npm
        run: sudo chown -R $USER:$GROUP ~/.npm

      - name: 🔐 Testar autenticação GitHub com GH_TOKEN
        run: |
          echo "Verificando autenticação via GitHub API com curl..."
          curl -H "Authorization: token $GH_TOKEN" https://api.github.com/user/repos
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: 🛠️ Configurar autenticação do Git com GH_TOKEN
        run: |
          git config --global user.name "semantic-release-bot"
          git config --global user.email "semantic-release@users.noreply.github.com"
          git remote set-url origin https://${GH_TOKEN}@github.com/${{ github.repository }}.git
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}

      - name: ✅ Verificar remote configurado
        run: git remote -v

      - name: 🚀 Rodar semantic-release
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          NPM_CONFIG_USERCONFIG: ~/.npmrc
        run: npx semantic-release
